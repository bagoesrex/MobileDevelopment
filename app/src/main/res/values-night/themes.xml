<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SkinCure" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="cardBackgroundColor">@color/bg_card_dark</item>
        <item name="strokeColor">@color/white</item>
        <item name="background">@color/black</item>
        <item name="android:statusBarColor">@color/primary</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.ButtonNavigation" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/bg_card_light</item>
        <item name="colorOnPrimary">@color/black</item>
    </style>
</resources>